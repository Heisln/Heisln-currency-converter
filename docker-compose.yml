version: '3.1'

services:
  currency-converter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9000:80
    image: 'currency-converter'
    container_name: 'currency-converter'